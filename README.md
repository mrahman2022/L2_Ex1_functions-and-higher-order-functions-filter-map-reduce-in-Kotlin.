# Kotlin Higher-Order Function Practice

## 📌 Description
This Kotlin program practices the use of:
- `filter`
- `map`
- `reduce`

Using a list of integers from **1 to 100**.

---

## 🧠 What the Program Does

### 1. `filter`
Gets all **even numbers** from the list.

### 2. `map`
Creates a list of **squares** of each number.

### 3. `reduce`
Calculates the **sum** of all numbers.

---

## ▶️ How to Run
You can run it using:

### **Kotlin Playground**
1. Go to https://play.kotlinlang.org
2. Paste the code from `Main.kt`
3. Click **Run**

### **Kotlin Compiler**
If installed on your PC:
```bash
kotlinc Main.kt -include-runtime -d app.jar
java -jar app.jar
